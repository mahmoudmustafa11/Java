package CircleArea;

public class CircleArea {
    public static void main(String[] args) {
        final double pi = 3.14;
        double sum, r;
        r = 2;
        sum = pi * Math.pow(r, 2);
        System.out.println("\nCircle area :\t" + sum);
    }    
}
